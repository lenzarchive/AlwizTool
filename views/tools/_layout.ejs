<!DOCTYPE html>
<html lang="<%= lang %>" class="">
<%- include('../partials/head') %>
<body class="bg-slate-50 dark:bg-slate-950 min-h-screen flex flex-col overflow-x-hidden">
<%- include('../partials/header') %>
<% var _tl = t.tools[tool.i18nKey] || {}; var _tc = t.common || {}; %>
<main class="flex-1 max-w-6xl mx-auto px-4 py-8 w-full">
  <nav class="flex items-center gap-2 text-xs text-slate-400 mb-6">
    <a href="/" class="hover:text-indigo-600 transition-colors"><%= _tc.back %></a>
    <span>/</span>
    <span class="text-slate-600 dark:text-slate-300"><%= _tl.title %></span>
  </nav>
  <div class="mb-6">
    <h1 class="text-2xl font-bold text-slate-900 dark:text-slate-100 mb-1"><%= _tl.title %></h1>
    <p class="text-sm text-slate-500 dark:text-slate-400"><%= _tl.subtitle %></p>
  </div>
  <%- body %>
  <section class="mt-10 pt-8 border-t border-slate-200 dark:border-slate-800">
    <h2 class="text-sm font-semibold text-slate-900 dark:text-slate-100 mb-4"><%= _tc.otherTools || 'Other Tools' %></h2>
    <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-3">
      <% tools.filter(function(tt) { return tt.slug !== tool.slug; }).slice(0, 8).forEach(function(tt) {
         var ttl = t.tools[tt.i18nKey] || {};
      %>
      <a href="/tools/<%= tt.slug %>" class="card p-3 group hover:border-indigo-300 dark:hover:border-indigo-700 transition-colors">
        <div class="flex items-center gap-2 mb-1">
          <span class="text-[10px] font-mono font-bold text-indigo-600 dark:text-indigo-400 w-7 shrink-0"><%= tt.icon %></span>
          <span class="text-xs font-semibold text-slate-700 dark:text-slate-300 group-hover:text-indigo-600 dark:group-hover:text-indigo-400 transition-colors"><%= ttl.title || tt.slug %></span>
        </div>
        <p class="text-[11px] text-slate-400 line-clamp-2 leading-relaxed"><%= ttl.subtitle || '' %></p>
      </a>
      <% }) %>
    </div>
  </section>
</main>
<%- include('../partials/footer') %>
<%- script %>
</body>
</html>
